import { Metadata } from "next";
import React from "react";
export const metadata: Metadata = {
  title: "Reports | Notice Generator",
  description: "Reports Page for Notice Generator",
};

const Reports = () => {
  return (
    <div>
      <h1>Reports</h1>
    </div>
  );
};

export default Reports;
